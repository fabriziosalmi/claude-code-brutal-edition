# Environment Configuration Template

# This file serves as a template for required environment variables.
# Copy this to .env and fill in your actual values.
# NEVER commit .env files with actual secrets to version control.

# ============================================================================
# CRITICAL: All credentials must be replaced with actual values
# Default or example values are NOT secure for production use
# ============================================================================

# Anthropic API Configuration
# Required: Your Anthropic API key for Claude Code functionality
# Get your API key from: https://console.anthropic.com/
ANTHROPIC_API_KEY=your_actual_api_key_here

# GitHub Integration (if using GitHub workflows/actions)
# Required for: Issue triage, PR reviews, automated workflows
# Scopes needed: repo, workflow
GITHUB_TOKEN=your_github_personal_access_token_here

# GitHub Repository Configuration
GITHUB_REPOSITORY=owner/repo-name

# Statsig Analytics (Optional)
# Required only if using analytics features
STATSIG_API_KEY=your_statsig_api_key_here

# Claude Code API Key Helper (Optional)
# TTL in milliseconds for API key caching
# Default: 3600000 (1 hour)
CLAUDE_CODE_API_KEY_HELPER_TTL_MS=3600000

# Dry Run Mode (Optional)
# Set to "false" to enable actual execution of operations
# Default: "true" for safety
DRY_RUN=true

# Issue Processing Limits (Optional)
# Maximum issue number to process in scripts
MAX_ISSUE_NUMBER=5000

# Minimum issue number to process in scripts  
MIN_ISSUE_NUMBER=1

# ============================================================================
# Security Best Practices
# ============================================================================
# 1. NEVER commit this file with actual secrets
# 2. Use strong, unique API keys (minimum 32 characters)
# 3. Rotate credentials regularly (every 90 days recommended)
# 4. Use separate credentials for development/staging/production
# 5. Enable MFA on all accounts that provide these credentials
# 6. Use secrets management tools (Vault, AWS Secrets Manager) in production
# 7. Audit API key usage regularly
# 8. Revoke unused or suspected-compromised keys immediately
# ============================================================================
